FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

ENV NODE_ENV=development
ENV VITE_API_BASE_URL=http://backend:8080

EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--host"] 